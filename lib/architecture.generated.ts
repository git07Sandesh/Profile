// Generated by scripts/diagrams.mts. Do not edit by hand.
// Source: docs/architecture/*.md

export const architectureDocs: Record<string, string> = {
  "clusterpath": "<h1>ClusterPath</h1>\n<p>Career direction is opaque. Which skills should you learn, and which roles actually fit the résumé you already have? ClusterPath takes a PDF and answers both with a clustered skills graph and a generated roadmap.</p>\n<h2>System</h2>\n<p><img src=\"/diagrams/clusterpath.svg\" alt=\"Architecture\"></p>\n<h2>Résumé to roadmap</h2>\n<ol>\n<li>A PDF is parsed into structured sections with PyMuPDF.</li>\n<li>Two extractions run over it: keywords via YAKE and KeyBERT, and vector embeddings of each section via Gemini.</li>\n<li>Jobs are matched by vector similarity, executed as database-side pgvector functions rather than pulled into application memory.</li>\n<li>Section embeddings are clustered with k-means to surface peer cohorts and adjacent roles.</li>\n<li>Matches and gaps feed a generated learning roadmap with skill-gap questions.</li>\n<li>The client renders the result as an interactive force-directed graph.</li>\n</ol>\n<h2>Stack</h2>\n<p><strong>Web:</strong> React 19 · Vite · React Router 7 · Tailwind · shadcn/ui · React Flow, sigma and d3 · Zustand · Supabase Auth, on Vercel.</p>\n<p><strong>API:</strong> FastAPI · PyMuPDF · Google Gemini (<code>embedding-001</code> and <code>gemini-2.0-flash</code>) · Supabase pgvector · KeyBERT · YAKE · scikit-learn, on Azure.</p>\n<h2>Decisions worth noting</h2>\n<p><strong>Similarity search lives in the database.</strong> Matching runs as pgvector functions inside Postgres, so candidate ranking never round-trips a large embedding set into Python. The store does the work it is good at.</p>\n<p><strong>Keywords and embeddings both, not either.</strong> Embeddings capture meaning and miss exact tokens; keyword extraction catches the literal terms a job description screens on. Running both and matching on the pair beats either alone.</p>\n<p><strong>Clustering answers a different question from matching.</strong> Matching says which jobs fit. Clustering over section embeddings says which <em>cohort</em> a résumé belongs to, which is what makes an adjacent-role suggestion useful rather than obvious.</p>\n<p><strong>A measured refactor.</strong> Moving to a utility-first design system cut the bundle by 60% and page load from 3.2s to 1.8s.</p>\n<p><em>Solo, full-stack. The <a href=\"https://github.com/git07Sandesh/clusterpath-api\">API is public</a>; the web client is currently private.</em></p>\n",
  "suga": "<h1>Suga</h1>\n<p>Support automation normally needs a knowledge base, a rules engine, and developers. Suga removes all three: an agent resolves one ticket by hand while a browser extension watches, an LLM turns that recording into an executable workflow, and a human approves it before it ever runs.</p>\n<h2>System</h2>\n<p><img src=\"/diagrams/suga.svg\" alt=\"Architecture\"></p>\n<h2>Capture to execution</h2>\n<ol>\n<li>A Chrome extension records the agent solving a real ticket: DOM events, screenshots, and element selectors captured with several fallback strategies so a workflow survives small UI changes.</li>\n<li>The extension posts the session unauthenticated and redirects the agent into the dashboard with a session id. It holds no credentials of its own.</li>\n<li>The backend analyses the session with Gemini and synthesises a draft workflow.</li>\n<li><strong>A human approves it.</strong> Nothing generated runs until someone signs off, and every approval is versioned with rollback.</li>\n<li>Approved workflows execute through an engine with per-step error strategies, queued on Redis, across eight third-party connectors.</li>\n<li>Tickets arrive over ten channels, with realtime chat, inbox, and call updates on separate socket namespaces.</li>\n</ol>\n<h2>Stack</h2>\n<p>Next.js 16 · React 19 · Fastify 5 · TypeScript · Supabase Postgres · BullMQ on Redis · Socket.IO · Google Gemini · Stripe · Chrome MV3 · LiveKit and Deepgram for voice.</p>\n<h2>Decisions worth noting</h2>\n<p><strong>Human-in-the-loop is the architecture, not a feature.</strong> The synthesis step produces a draft, never a live automation. That single boundary is what makes an LLM-authored workflow safe to run against a customer&#39;s tooling, and it is why hallucination cannot reach production.</p>\n<p><strong>The recorder holds no credentials.</strong> It captures and hands off, then gets out of the way. Authentication begins in the dashboard, which keeps the extension&#39;s blast radius small.</p>\n<p><strong>Connectors and channels are different axes.</strong> Eight connectors are what a workflow can <em>act on</em>; ten channels are where tickets <em>arrive from</em>. The engine&#39;s node registry also contains primitives — HTTP, inline code, variable assignment — which are node types rather than integrations.</p>\n<p><strong>Failure is per step.</strong> Each step declares stop, skip, or retry, so one flaky third-party call does not discard an entire run.</p>\n<p><em>Founding engineer. Second of thirty-plus at the USM-VOXO Hackathon.</em></p>\n",
  "transworld": "<h1>Transworld Floor Plan</h1>\n<p>A trade show&#39;s exhibit floor plan was a static PDF. Attendees could not navigate it and it produced no sponsorship surface. This replaced it with a live, searchable, interactive map serving 441 exhibitors, built solo from an empty repository to production under a hard deadline.</p>\n<h2>System</h2>\n<p><img src=\"/diagrams/transworld.svg\" alt=\"Architecture\"></p>\n<h2>How a floor plan reaches the screen</h2>\n<ol>\n<li>A request arrives and middleware resolves the host to a tenant. Several trade show brands run from one codebase.</li>\n<li>In production that same middleware applies an allowlist, so the deployment exposes the floor plan and little else.</li>\n<li>The show route asks an API route for the published plan; the server reads it through the Firebase Admin SDK rather than exposing the database to the client.</li>\n<li>Booths and zones render onto a Konva canvas with zoom, pan, and hit-testing.</li>\n<li>Selecting a booth opens an exhibitor panel; a separate route records the interaction, which is what turns the map into a sponsorship surface.</li>\n</ol>\n<h2>Stack</h2>\n<p>Next.js 16 App Router · React 19 · TypeScript · Tailwind v4 · Zustand · Konva · Firebase Auth and Firestore · Cloudflare R2 · Resend · PostHog · a Cloud Run service for booth detection during authoring.</p>\n<h2>Decisions worth noting</h2>\n<p><strong>Multi-tenant by host, not by build.</strong> One deployment serves multiple show brands. Tenant identity is resolved per request from the incoming host, with development overrides for subdomains, so a new show is configuration rather than a fork.</p>\n<p><strong>The database is never reached from the browser.</strong> Published plan reads go through a server route using the Admin SDK. Roles are Firebase custom claims set server-side, so a client cannot promote itself.</p>\n<p><strong>Canvas, not DOM, for the map.</strong> Hundreds of booths with zoom and pan is the case where per-node DOM stops being viable. Konva keeps interaction smooth while the surrounding UI stays ordinary React.</p>\n<p><strong>Analytics proxied through the app&#39;s own origin</strong>, which keeps measurement working when third-party requests are blocked.</p>\n<p><em>Client work for Arroyodev LLC. Documented at the architectural level; no proprietary schema, credentials, or internal identifiers are included.</em></p>\n",
};
